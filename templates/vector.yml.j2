data_dir: /var/lib/vector

sources:
  app_logs:
    type: file
    include:
      - /var/log/**/*.log

sinks:
  clickhouse:
    type: clickhouse
    inputs:
      - app_logs
    endpoint: http://{{ hostvars['clickhouse-01']['ansible_host'] }}:8123
    database: logs
    table: vector_logs
